# 更新日志

本文件记录主要版本的变更；更早的历史可直接查看 `git log`。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)（`主版本.次版本.修订号`）。

## 未发布

### WebDAV

- `PROPFIND Depth: 1` 改为按 256 项分页，并以目录优先 keyset 游标继续读取；文件属性按页批量读取，避免一次装载整目录及逐文件查询。
- 覆盖根目录、嵌套目录及跨目录/文件游标边界的高扇出回归测试。

## v2.6.7

补齐 `vfiles ci` 的目录复制能力。

### 改进

- `vfiles ci` 现在同时接受普通文件和目录；目录复制会递归保留子目录与空目录，跳过符号链接，并遵守服务端单文件大小限制。
- 目录使用批量导入模式减少快照频率；遇到部分失败时仍提交已完成的变更，再以非零状态报告错误。

## v2.6.6

新增面向本机用户空间的基础文件操作 CLI。

### 改进

- **`vfiles ls`**：列出用户空间目录的直接文件与子目录。
- **`vfiles ci`**：将本地普通文件流式复制到用户空间指定路径并提交快照。
- **`vfiles co`**：将用户空间文件或目录树复制到本地路径。
- 三个命令支持 `--owner <用户名>`；未指定时使用默认命名空间所属用户。

## v2.6.5

rsync 归档目录时间戳兼容补丁版本。

### 改进

- **目录 mtime 保真**：rsync `-a` 推送时持久化目录源修改时间，并在子项和删除操作完成后应用；拉取目录清单会返回保存的时间戳。

### 升级说明

- 启动时自动增加目录源 mtime 数据列；既有目录保持未设置状态，列表继续使用原有创建时间回退值。

## v2.6.4

rsync 元数据查询内存与读取范围优化补丁版本。

### 改进

- **rsync push stat 缓存**：由全命名空间文件元数据缓存改为按父目录懒加载，并限制连接级缓存目录数；对小范围同步大型命名空间减少无关元数据读取和常驻内存。

## v2.6.3

WebDAV URI 健壮性与 S3 multipart 升级兼容补丁版本。

### 修复与改进

- **WebDAV URI 解码健壮性**：无效百分号转义后跟原始多字节 UTF-8 时不再导致服务 panic。
- **S3 multipart 升级兼容**：启动时一次性回填升级前仍在进行的上传会话，避免新索引启用后旧 multipart 上传从列表中消失；回填可幂等重试。

### 升级说明

- 启动时自动创建 S3 multipart 回填状态迁移；首次启用 S3 时会扫描并登记旧上传会话。

## v2.6.2

协议可靠性与性能修复补丁版本。

### 修复与改进

- **S3 multipart 列表索引**：通过 SQLite 索引分页读取分片上传会话，避免遍历全部文件上传目录；创建失败、完成与取消时同步维护索引。
- **凭证缓存即时撤销**：缓存命中后复核用户当前密码哈希与禁用状态，管理员重置密码或禁用账号后，旧凭证立即失效。

### 升级说明

- 启动时自动创建 S3 multipart 索引迁移；升级前建议备份数据库和数据目录。

## v2.6.1

历史记录性能与分页修复版本。

### 修复与改进

- **历史记录游标分页**：文件历史超过首批结果后可以继续加载，修复记录超过接口上限后「加载更多」不再前进的问题。
- **历史查询性能**：文件版本历史、版本差异、目录历史和历史快照目录浏览均改为有界读取或 SQLite 分页查询。
- **快照访问隔离**：阻止跨命名空间访问其他用户的快照内容、目录树和归档。

## v2.6.0

WebDAV 大目录兼容性与性能改进版本。

### 新增与改进

- **PROPFIND Depth: infinity 分页**：SQLite 按路径游标分批读取子树，并逐批加载属性、版本和锁信息及序列化响应，减少大目录遍历时的峰值中间数据占用。
- **分页回归覆盖**：增加子树跨页连续性验证；Litmus 104 项、cadaver 与 rclone WebDAV 客户端实测通过。

## v2.5.0

协议兼容性与可靠性版本，重点提升 WebDAV、S3 和 rsync 的真实客户端互操作、并发写入安全与大对象处理。

### 新增与改进

- **WebDAV 与 HTTP 共端口**：默认挂载在 `/dav`；S3 默认共用 HTTP 端口并挂载在 `/s3`，可分别通过配置关闭。
- **WebDAV 写入并发保护**：PUT、PROPPATCH、DELETE、MOVE、COPY 在存储事务提交时复核锁状态与资源条件，避免长上传或锁竞争期间绕过锁。
- **WebDAV 浏览器与客户端发现**：共端口服务支持 DAV CORS 预检；普通 OPTIONS 保留 DAV/Allow 能力声明。cadaver、rclone 与 Litmus 的 104 项测试覆盖相关操作。
- **S3 key 保真与稳定性**：保留前后斜线等 S3 key 身份，改进列表分页、删除标记、条件操作及对象元数据提交；覆盖 boto3/botocore 58 项操作和 rclone 1005 对象分页。
- **rsync 错误可靠性**：上传或 `--delete` 阶段失败会正确传回非零退出状态；真实 rsync 客户端验证递归推拉、归档时间戳、增量传输与删除过滤。
- **HTTP 上传与归档改进**：限制 multipart 元数据的单字段和总量，并批量读取 ZIP 历史版本元数据。

### 升级说明

- 数据库迁移会在启动时自动执行，包含 S3 key 索引及 WebDAV 锁相关迁移；升级前仍建议备份数据库和数据目录。
- S3/rsync 默认启用的部署请配置固定凭证及访问策略；不需要的协议可通过对应环境变量关闭。

## v2.4.2

布局修复补丁版本（两个界面遮挡问题）。

### 修复

- **目录树不再盖住状态栏**：目录多时整棵「全部文件」树按内容撑高、顶出侧栏，
  部分行看不到也无法滚动（滚动条不出现）。现在树在侧栏内内部滚动并保留最小高度，
  矮窗口下概览块会压缩并自行滚动，状态栏与概览都不会被遮挡。
- **视图/排序弹层完整显示**：弹层此前向左展开，会越过卡片左缘被裁掉一截（桌面左侧约 68px）。
  桌面改为左缘对齐触发器向右展开；移动端弹层在搜索行宽内居中，任何按钮位置都不会被裁。

## v2.4.1

防误删补丁版本。

### 修复

- **单条删除现在需要二次确认**：此前右键菜单、详情面板、移动端长按菜单与
  仅高亮时的 Delete/Backspace 键点击即删（多选批量删除一直有确认）。
  确认框会写明「删除后不可撤销」，目录删除额外提示「目录及其全部内容将被删除」，
  取消则不发出任何删除请求。

## v2.4.0

管理员账号恢复版本：忘记账号或密码时，可以完全离线地找回入口，不再需要手工改库。

### 新增功能

- **`vfiles user reset-password`**：离线重置密码（不依赖邮件/SMTP）
  - `--username` / `--user-id` 定位账号，会先打印 id / 用户名 / 邮箱 / 角色 / 状态便于确认；
  - `--password` 指定新密码（至少 8 位）、`--generate` 生成随机强密码并打印、
    `--password-hash` 直接写入哈希（自动化）；
  - 默认**撤销该用户全部登录会话**，`--keep-sessions` 可保留。
- **`vfiles user update --username`**：支持改名（修复历史数据里的非法用户名）。

### 修复

- **首个管理员的用户名不再绕过校验**：此前 `vfiles user create --username root-admin`
  会把含 `-` 的用户名写入库，随后读取失败（`Internal error: Invalid username`），
  导致用户列表、按名查找与登录全部不可用；现在 bootstrap 与普通创建一致校验。
- **历史数据读取宽容**：新增 `Username::from_stored`，一条未通过校验的旧记录不再让
  整表用户读取失败，这类账号可以被列出、重置密码并改名修复。

### 升级说明

- 本版本**不包含数据库迁移**（仅新增 CLI 能力与读取兼容），升级只需替换二进制；
- 若你的实例已经因为非法用户名的首个管理员而无法列用户/登录，升级后按
  `docs/ADMIN_SETUP.md`「忘记管理员账号 / 密码」一节修复即可。

## v2.3.0

本次为功能与体验版本，重点补齐了「协作/自动化」能力，并做了一轮界面一致性迭代。

### 新增功能

- **审计日志**（只读）：记录登录、上传、下载、分享、转移等关键操作，包含访问 IP 与设备类型；
  支持关键字/动作/结果/时间范围筛选、**按用户与动作聚合概览**、**CSV 导出**（含汇总区块），
  数据库触发器保证日志不可修改、不可删除，仅管理员可读。
- **转移所有权**：把文件或目录（含整棵子树）转给另一个用户，**版本历史一起转移**；
  双方各留一条版本记录，冲突/自转/根目录等场景有明确报错，全过程写入审计日志。
- **访问令牌**：给 CLI、CI 等程序使用的 API 凭证（`Authorization: Bearer vfat_…`），
  明文只显示一次、库中只存 SHA-256 摘要，支持有效期与撤销；令牌无法创建/撤销令牌。
- **单请求上传接口**：`PUT /api/files/upload`（含 URL 路径形式 `PUT /api/files/upload/<路径>`），
  可直接配合 `curl -T`、`curl --data-binary` 使用，无需 init/分片流程。
- 分享有效期新增「一年」「永久」选项；分享链接访问次数与最近访问信息可见。

### 改进

- **文件列表交互**：PageUp/PageDown 翻页（网格按「可视行数 × 列数」）、按键定位（type-ahead）、
  空格切换选中、快捷键面板（`?`）、Escape 逐层退出。
- **加载状态统一**：列表/侧栏/预览/版本历史/移动对话框/管理页统一使用同一套骨架屏组件。
- **通知中心**：顶栏铃铛回看错过的提示，支持未读角标、按类型筛选、单条移除与清空。
- **移动端**：搜索头部吸顶、类型 chips 单行横向滚动；底部操作栏与提示条位置优化。
- **分享/审计/用户管理/访问令牌**四个页面页头操作统一（刷新 / 返回文件），并统一了卡片语言。
- 网格缩略图骨架占位 + 淡入；对话框层级修正（嵌套确认框不再被遮挡）。
- 上传体积校验：`VFILES_MAX_FILE_SIZE_MB`（单文件上限）现在对**所有**上传路径在服务端强制生效。

### 修复

- 分享页「停止分享」调用了错误路径（命中前端回退返回 200），实际未停用链接；
- 所有者无法停止已过期链接（后端按过期时间过滤导致 404）；
- 审计日志列表筛选参数被前端丢弃（页面始终显示全部）；单条畸形时间戳会导致整表读取失败；
- 审计导出/令牌接口的时间戳格式不是 RFC3339，界面显示原始字符串；
- 令牌鉴权与上传上限等若干后端一致性问题。

### 升级说明

- 数据库迁移会自动执行（新增 `0004_audit_logs.sql`、`0005_access_tokens.sql`），无需手工操作；
- 若从 v2.2.0 升级，单文件上限的**服务端强制**是新行为，请确认 `VFILES_MAX_FILE_SIZE_MB` 配置符合预期；
- 单文件模式（`--features embed`）构建方式不变，详见 `docs/DEPLOYMENT.md`。
